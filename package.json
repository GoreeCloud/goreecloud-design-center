{
  "name": "@goreecloud/design-center",
  "version": "0.1.0-development",
  "private": true,
  "description": "GoreeCloud Design Center development foundation and Glaze UI reference workspace.",
  "type": "module",
  "scripts": {
    "sync:glaze": "node scripts/tooling.mjs sync",
    "validate": "node scripts/tooling.mjs validate && node scripts/validate-workspace.mjs",
    "build": "node scripts/tooling.mjs build && node scripts/validate-workspace.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
